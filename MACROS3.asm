PRINT MACRO	CHAR
	PUSH AX
	PUSH DX
	MOV DL,CHAR
	MOV AH,2
	INT 21H
	POP DX
	POP AX
ENDM

READ MACRO 
	MOV AH,8
	INT 21H
ENDM

PRINT_STR	MACRO CHAR
	PUSH AX
	PUSH DX
	MOV DX,OFFSET CHAR
	MOV AH,9
	INT 21H
	POP DX
	POP AX
ENDM

EXIT MACRO
	MOV AX,4C00H
	INT 21H
ENDM

INIT MACRO
	MOV AX,DATA_SEG
	MOV DS,AX
ENDM